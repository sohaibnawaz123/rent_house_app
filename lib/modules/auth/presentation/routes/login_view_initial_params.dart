class LoginViewInitialParams {
  const LoginViewInitialParams();

  factory LoginViewInitialParams.fromUri(Uri uri) {
    return LoginViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}