class RefreshtokenViewInitialParams {
  const RefreshtokenViewInitialParams();

  factory RefreshtokenViewInitialParams.fromUri(Uri uri) {
    return RefreshtokenViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}