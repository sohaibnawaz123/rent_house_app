class ResetpasswordViewInitialParams {
  const ResetpasswordViewInitialParams();

  factory ResetpasswordViewInitialParams.fromUri(Uri uri) {
    return ResetpasswordViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}