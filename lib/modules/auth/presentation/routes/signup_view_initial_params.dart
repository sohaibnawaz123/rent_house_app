class SignupViewInitialParams {
  const SignupViewInitialParams();

  factory SignupViewInitialParams.fromUri(Uri uri) {
    return SignupViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}