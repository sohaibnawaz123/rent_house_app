class RegisterViewInitialParams {
  const RegisterViewInitialParams();

  factory RegisterViewInitialParams.fromUri(Uri uri) {
    return RegisterViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}