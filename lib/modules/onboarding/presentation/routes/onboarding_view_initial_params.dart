class OnboardingViewInitialParams {
  const OnboardingViewInitialParams();

  factory OnboardingViewInitialParams.fromUri(Uri uri) {
    return OnboardingViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}