class AboutViewInitialParams {
  const AboutViewInitialParams();

  factory AboutViewInitialParams.fromUri(Uri uri) {
    return AboutViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}