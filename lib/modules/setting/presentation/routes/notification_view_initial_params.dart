class NotificationViewInitialParams {
  const NotificationViewInitialParams();

  factory NotificationViewInitialParams.fromUri(Uri uri) {
    return NotificationViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}