class PaymentcardViewInitialParams {
  const PaymentcardViewInitialParams();

  factory PaymentcardViewInitialParams.fromUri(Uri uri) {
    return PaymentcardViewInitialParams(
      // Parse the URI parameters as needed
    );
  }
}